{
  "name": "osk-webhook-relay",
  "version": "1.0.0",
  "description": "Receives POST webhooks, logs them with size-based rotation/gzip, and relays them to a GHL webhook",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^9.0.5"
  }
}
